{
  "name": "notification-nodejs",
  "version": "1.0.0",
  "description": "Node.JS interface to Google's Firebase Cloud Messaging (FCM). Supports both android and iOS",
  "main": "server.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "Shailendra Sen",
  "license": "ISC",
  "dependencies": {
    "express": "^4.16.3",
    "firebase-admin": "^5.12.1"
  }
}
